# PRD — World of Tomorrow 딥리서처

## 1. 한 줄 요약
나와 과제 평가자를 위해, **1939 뉴욕세계박람회가 미래의 도시와 일상생활을 어떻게 상상했는가**를 분야별 서브에이전트들이 나눠 읽고 장문 보고서로 엮는 딥리서치 에이전트.

## 2. 해결할 문제
- AI의 발전으로 미래에 대한 관점이 격변하고 있는 지금, 과거의 사람들은 미래를 어떻게 보았고 현재와는 어떻게 다른지 다시 살펴보고, 우리가 미래를 어떻게 생각해야 하는지 도움을 받고 싶다.
- 2차 대전 직전에 치러진 1939 뉴욕세계박람회가 시기적으로 의미 있고 자료도 찾기 쉽다.
- 단순히 박람회 역사나 전시 목록을 요약하는 것이 목적이 아니다. 조직자와 기업들이 전시·공식 자료로 **서로 다른 미래상을 어떻게 제시했는지**를 조사한다.
- 관련 자료는 한 번에 모델 창에 다 넣을 수 없을 만큼 많다. 그래서 분야별로 나눠 읽는 분업(오케스트레이션)이 필요하다.

## 3. 사용자
- **주 사용자**: 나(혼자 쓰는 프로그램). 로컬에서 질문을 넣고 보고서를 만든다.
- **보조 사용자**: 과제 평가자. 공개 주소에서 **미리 돌려 둔 보고서와 추적 화면만** 본다.
- 로그인은 필요 없다.

## 4. 핵심 기능

| 기능 | 설명 | 우선순위 |
|---|---|---|
| 코퍼스 수집 | 1939 박람회 관련 위키백과 문서 30건 이상과 문서 간 링크를 `data/corpus.json`에 저장. 총 글자 수가 모델 창을 넘는지 숫자로 확인 | 필수 |
| 질문 세트 | 질문 8건 이상, 성격을 섞음(한 건으로 답이 나오는 것 / 여러 갈래 / 한 대상 추적). 질문마다 "왜 나눌 만한가" 한 줄. 정답표는 만들지 않음 | 필수 |
| 기획(코디네이터) | **분야별**로 목차를 짬(예: 자동차와 교통 / 미래 도시와 인프라 / 가정과 일상생활). 절마다 역할·시작 문서·읽기 예산을 배정. 배정한 문서가 실제로 있는지 코드가 검사 | 필수 |
| 배치(서브에이전트) | 절마다 서브에이전트를 동시에 파견하고, 남의 구역을 알려 줘 중복 읽기를 막음. 자료 탐색은 **시작 문서에서 링크를 따라가는 그래프 방식**. 원문은 아래에 남고 위로는 원고만 올라감 | 필수 |
| 격리 증명 | 코디네이터가 본 글자 수와 서브에이전트가 본 글자 수를 따로 셈 | 필수 |
| 점검 | 부족하다고 스스로 신고한 절만 다시 내보냄. 바퀴 수 상한. 두 번째 원고 처리 규칙과 이유를 적음 | 필수 |
| 종합 | 절 원고를 이어 붙여 장문 보고서 작성 | 필수 |
| 지표 | 정답표·판정 모델 없이, 만들어진 글과 실제로 읽은 자료만 봄. 신호(높낮이 비교)와 경보(0이어야 함)를 구분. 지표마다 어느 장치를 보는지 명시 | 필수 |
| 실험 | 기본 / 장치를 하나 끈 것(ablation) / 혼자 하는 대조군(같은 자료·모델·도구·읽기 예산). 대조군이 예산을 다 쓰는지 확인 | 필수 |
| **실행 전 계획 보고** | 실행할 때마다 절 구성, 서브에이전트별 역할·시작 문서·읽기 예산, 예상 호출 수·토큰을 먼저 보여 주고 **승인 후 실행** | 필수 |
| 웹 데모 | 질문 입력 → 계획 확인 → 실행 → 보고서 + 절마다 "누가 무엇을 읽고 무엇을 썼는지" | 필수 |
| 읽은 문서 관계도 | 서브에이전트별로 읽은 문서를 색으로 구분한 링크 그래프 **(제가 정했습니다)** | 필수 |
| 공개 보기 모드 | 공개 주소에서는 미리 돌려 둔 결과만 보여 줌. API 호출 없음 | 필수 |
| 워드클라우드 | "설명이 한 줄로 안 되는 지표는 버린다"는 원칙에 맞지 않아 제외 **(제가 정했습니다)** | 안 함 |

## 5. 사용 흐름

**로컬(나)**
1. 질문을 입력하거나 질문 세트에서 고른다.
2. 코디네이터의 계획(목차·역할·시작 문서·예산·예상 호출 수와 토큰)을 확인하고 승인한다.
3. 서브에이전트들이 동시에 돈다. 끝나면 점검을 거쳐 보고서가 나온다.
4. 보고서와 절별 추적(읽은 문서, 원고, 관계도, 지표)을 본다. 결과는 `output/`에 저장된다.

**공개(평가자)**
1. 공개 주소에 접속한다.
2. 미리 돌려 둔 질문 목록에서 하나를 고른다.
3. 보고서와 절별 추적을 본다.

## 6. 데이터
전부 저장소 안의 파일로 둔다. DB는 쓰지 않는다.
- `data/corpus.json`: 문서(docs)와 링크(links)
- `data/questions.json`: 질문과 "왜 나눌 만한가"
- `config.json`: 절 수, 절 예산, 바퀴 상한, 역할 명단
- `output/runs.jsonl`, `output/ablation.json`, `output/reports/`: 실행 기록과 보고서. 공개 모드는 이 파일만 읽는다.
- API 키는 환경변수로만 다루고 저장소에 올리지 않는다.

## 7. 기술 & 환경
- **Python 3.11 + LangGraph**: 과제가 LangGraph 노드·상태 구조도를 요구하고, 이미 설치돼 있다.
- **OpenAI gpt-4o-mini**: 반복 실험에 드는 비용을 최소로 하기 위해서다.
- **Streamlit**: 이미 설치돼 있고 브라우저에서 돌아간다. Community Cloud로 공개 배포도 가능하다.
- **자료 출처 = 영어 위키백과 API** **(제가 정했습니다)**: 1939 박람회 문서가 영어판에 가장 많고, 문서끼리 링크가 걸려 있다.
- **탐색 = 그래프(링크 따라가기)**: 시작 문서를 배정받고 링크를 따라가기만 하므로 검색 범위가 최소로 유지된다.

## 8. 완성 기준
- [ ] `corpus.json`에 문서 30건 이상과 links가 있고, 총 글자 수와 모델 창 크기를 비교한 숫자가 출력된다.
- [ ] `questions.json`에 질문 8건 이상이 있고, 한 건으로 답이 나오는 질문이 포함돼 있다.
- [ ] 실행 전에 계획(목차·역할·시작 문서·예산·예상 호출 수와 토큰)이 보고되고, 승인해야 실행된다.
- [ ] 코디네이터가 분야별 목차를 짜고, 존재하지 않는 문서를 배정하면 코드가 걸러낸다.
- [ ] 서브에이전트가 동시에 돌고, 코디네이터와 서브에이전트가 본 글자 수가 따로 기록된다.
- [ ] 부족 신고 절만 재파견되고, 바퀴 상한에서 멈춘다.
- [ ] `metrics.py`가 정답표 없이 신호와 경보를 계산한다.
- [ ] 기본 / ablation / 대조군 보고서가 같은 질문으로 만들어지고, 대조군의 예산 소진이 확인된다.
- [ ] 로컬에서 `streamlit run app.py`로 질문 → 계획 승인 → 보고서 → 절별 추적이 된다.
- [ ] 공개 모드에서 API 호출 없이 저장된 결과를 볼 수 있다.
- [ ] `README.md`에 실행 방법과 화면 캡처, `REPORT.md`에 가이드 6항목이 있다.
- [ ] **기한: 지금부터 3시간 안에 제출**

## 9. 이번에 안 하는 것
- 로그인, 사용자 계정
- 공개 주소에서 방문자가 새 질문을 실행하는 것
- 검색(임베딩·키워드) 기반 탐색
- 워드클라우드
- 위키백과 이외의 자료 출처(1차 사료 스캔, 신문 아카이브 등)
- 정답표, 판정용 LLM

## 10. 미정 사항
- **목차의 분야 명단**: 예시로 든 3분야(자동차와 교통 / 미래 도시와 인프라 / 가정과 일상생활)에 더 넣을지(예: 통신·미디어, 과학기술·로봇). 코퍼스를 모은 뒤, 문서가 분야별로 고르게 나뉘는지 보고 정합니다.
- **총 글자 수가 모델 창(128k 토큰)을 넘는지**: 1939 박람회 문서만으로 부족하면 인물·기업·건축 양식 문서로 넓혀야 합니다. 수집 직후 숫자로 확인합니다.
- **절 예산·바퀴 상한의 숫자**: 계획 보고 때 예상 비용과 함께 제안하고 승인을 받습니다.
- **공개 배포 여부**: 과제에서는 선택입니다. 3시간 기한 안에 로컬 완성이 먼저이고, 시간이 남으면 합니다.
- **관계도 외 화면 요소**: 관계도로 정했지만(제가 정했습니다) 아직 고민 중이시니, 데모 단계 전에 다시 확인합니다.
